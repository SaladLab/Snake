using System;

namespace Domain
{
    public static class Rule
    {
        public static readonly int BoardWidth = 23;
        public static readonly int BoardHeight = 28;
        public static readonly TimeSpan SnakeSpeed = TimeSpan.FromSeconds(0.2);
    }
}
